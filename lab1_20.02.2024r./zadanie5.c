
#include <stdio.h>

int main()
{
    int max, wynik;
    printf("Podaj ograniczenie: ");
    scanf("%d", &max);
    
    for(int i == 1; i<max; i ++)
    {
        if(i%3==0 && i%5==0)
        else if(i%5==0) wynik+=i;
        else if(i%3) wynik +=i;
    }
    
    return 0;
}

//dokończyć
